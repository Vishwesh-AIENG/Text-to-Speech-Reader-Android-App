<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera: required for CameraX live preview and frame analysis -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Internet: required for ML Kit Translation model downloads and Gemini cloud summarization (HTTPS only) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network state: used by NetworkMonitor to detect offline conditions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Microphone: required by Babel Engine for on-device speech recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Hardware features. Camera is required (all OCR modes depend on it). Mic is optional
         so devices without a microphone can still install and use non-Babel modes. -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:name=".OmniLingoApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OmniLingo"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
